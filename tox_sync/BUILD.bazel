cc_binary(
    name = "tox_sync",
    srcs = ["tox_sync.c"],
    deps = [
        "//c-toxcore",
        "//toxins/util:misc_tools",
    ],
)

sh_test(
    name = "tox_sync_test",
    srcs = [":tox_sync"],
    args = ["--help"],
)
